how to use:
script + config, e.g.:
submit.sh ./configs/rubcnl_sanity_N19
l40submit.sh ./configs/all_sanity_N19
use e.g. l40 that has more vram for the all genes runs with n19, pay attention to which encoder etc is uses so that e.g. 17k genes model fits into the vram